{
  "name": "example",
  "version": "0.0.1",
  "main": "index.js",
  "private": true,
  "scripts": {
    "clean": "npx tsc --build --clean",
    "build": "npx tsc --build",
    "start": "yarn build && node ./dist/index.js"
  },
  "dependencies": {
    "@polkadot/api": "15.2.1",
    "@polkadot/keyring": "13.3.1"
  }
}
